#pragma once

enum SELECT_OPTION
{
    SELECT_QUIT = 0,
    SELECT_ROCK,
    SELECT_SCISSOR,
    SELECT_PAPER,
    SELECT_GAMES_SUMMARY,
    SELECT_GAMES_ANALYSIS,
    SELECT_MAX
};

enum RESULT
{
    RESULT_UNDEFINED = 0,
    WIN,
    LOST,
    DRAW
};
